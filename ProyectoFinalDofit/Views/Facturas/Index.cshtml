@model IEnumerable<ProyectoFinalDofit.Models.Facturas>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Numero_Factura)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fecha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Proveedores.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tipo_Movimientos.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Trabajadores.Codigo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Numero_Factura)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Proveedores.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tipo_Movimientos.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Trabajadores.Codigo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Factura_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Factura_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Factura_Id })
        </td>
    </tr>
}

</table>
